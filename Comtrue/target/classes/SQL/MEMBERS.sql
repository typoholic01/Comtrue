-- 테이블 삭제
DROP TABLE MEMBERS
CASCADE CONSTRAINT;

DROP SEQUENCE MEMBER_NO;

--직원 정보 테이블
CREATE TABLE MEMBERS(
    MEMBER_NO NUMBER(3) PRIMARY KEY,
    NAME VARCHAR2(50) NOT NULL,
    PHONE VARCHAR2(50) NOT NULL,
    RANK VARCHAR2(50) NOT NULL,
    EMAIL VARCHAR2(50) NOT NULL
)

-- USER_ID 생성용 시퀀스
CREATE SEQUENCE MEMBER_NO START WITH 1 INCREMENT BY 1; 

-- 조회용
SELECT * FROM MEMBERS;

--데이터 수정
UPDATE MEMBERS
	SET (MEMBER_NO, NAME, PHONE, RANK, EMAIL) 
	= 
	
	'문성환'
WHERE MEMBER_NO = 1

UPDATE MEMBERS
	SET MEMBER_NO = 2, 
	SET NAME = '문성환',
	SET PHONE = '01022654988',
	SET RANK = '사원',
	SET EMAIL = 'typoholic01@gmail.com'
WHERE MEMBER_NO = 1